# sdd-eval

Map only.

- `docs/eval.md` — winner rule. This pin is a microbench. A self-authored fixture cannot KEEP the cookbook.
- `fixtures/product/` — subject fixture (cratewake). Exists to be measured. Not EditLayer, PhotoSocial, washere.
- `sdd-eval-tasks.yaml` — hashed tasks
- `src/run.ts` — harness. This code is the architecture.

No winner in this file.
