# eval

Product probe notes for cratewake. Not the research winner rule. Not KEEP.

## Command

`npm run eval:gold` writes `evals/gold.jsonl`. Sample floor `probe-count 88`.

## SLOs

Wake `p99-wake 45ms`. `error-budget 0.7%`. Scrape `scrape 15s`. Metric prefix `cratewake_dock_`. Alert file `alerts/dock-lease.yaml`.
