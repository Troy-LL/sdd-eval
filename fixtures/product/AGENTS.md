# cratewake

Map only. This tree exists to be measured. Not a shipping product.

- `README.md` — product limits, paths, commands
- `docs/architecture.md` — process, sockets, replicas, spill
- `docs/design.md` — operator UI
- `docs/eval.md` — SLOs, scrape, gold command

Load this file + 2, or + 3 because `docs/eval.md` exists. Pin order when loading more than one: README → architecture → design → eval. Cite paths. Do not paste.
